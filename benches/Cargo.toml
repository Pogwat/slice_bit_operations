[package]
name = "benchmark"
version = "0.1.0"
edition = "2024"
repository = "https://github.com/Pogwat/slice_bit_operations"

[dependencies]
slice_bit_operations = { path = ".." }
criterion = "0.8.2"

[[bench]]
name = "benchmarks"
harness = false
path = "benchmarks.rs"
